import PropTypes from 'prop-types';
const VideoListItem = ({ thumbnail, name }) => {
  return <div></div>;
};
VideoListItem.propTypes = {
  thumbnail: PropTypes.string.isRequired,
  name: PropTypes.string.isRequired,
};
export default VideoListItem;
